{
  "name": "digital-product-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix storefront install && npm --prefix admin install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:storefront": "npm --prefix storefront run dev",
    "dev:admin": "npm --prefix admin run dev -- -p 3001",
    "dev": "npx --yes concurrently -n \"backend,storefront,admin\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:storefront\" \"npm run dev:admin\""
  }
}
