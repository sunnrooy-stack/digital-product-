services:
  - type: web
    name: digital-product-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node index.js
    envVars:
      - key: PORT
        value: "5000"
      - key: NODE_ENV
        value: "production"
